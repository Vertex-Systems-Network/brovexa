# Brovexa M01 local development
# Copy to .env for local development. Never commit real secrets.

NODE_ENV=development
HOST=0.0.0.0
PORT=3001
# Leave empty to run the API without database readiness. Local Compose URL is documented in docs/DATABASE.md.
DATABASE_URL=
